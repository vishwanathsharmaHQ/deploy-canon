/** API base URL — relative because frontend is served by backend. */
export const API_BASE_URL = '/api';

/** Debounce delay (ms) for auto-save operations. */
export const AUTOSAVE_DEBOUNCE_MS = 1000;

/** Max semantic search results. */
export const SEMANTIC_SEARCH_LIMIT = 20;

/** Word count threshold to trigger semantic search instead of substring. */
export const SEMANTIC_WORD_THRESHOLD = 3;
